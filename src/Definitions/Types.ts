/**
 * Fixate possible directions of a moving game object.
 */
export type Direction = "up" | "down" | "left" | "right";

/**
 * Sides of a shape that can be hit.
 */
export type hitSide = "top" | "bottom" | "left" | "right" | undefined;